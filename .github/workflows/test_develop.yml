name: Run tests on Develop Pull Request
on:
  pull_request:
    branches: [develop]
jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.1.2
      - run: |
          git fetch --prune --unshallow
      - name: "Populate auth file with SFDX_URL secret of integration org"
        shell: bash
        run: |
          echo ${{ secrets.URL}} > ./SFDC_URL.txt
      - name: Authenticate with Salesforce
        uses: marcelblijleven/salesforcedx-actions@master
        with:
          args: force:auth:sfdxurl:store --sfdx-url-file=./SFDC_URL.txt --alias=develop
      - name: Run Apex tests
        uses: marcelblijleven/salesforcedx-actions@master
        with:
          args: force:apex:test:run -l RunLocalTests -w 10 -o develop
